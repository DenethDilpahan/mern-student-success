import React from 'react';

const TrackProgress = () => {
  return (
    <div>
      <h2>Track Progress</h2>
      <p>This section will handle progress tracking (CRUD operations).</p>
    </div>
  );
};

export default TrackProgress;